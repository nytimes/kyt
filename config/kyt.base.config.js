module.exports = {
  clientPort: 3001,
  serverPort: 3000,
  prototypePort: 3333,
  debug: false,
  // editWebpackConfig: (baseConfig, callback) => {
  //   // Does some stuff
  //   callback(newConfig); // how should we expose this?
  // }
};

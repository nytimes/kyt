
// This is your prototyping file.
// It's the entry for the kyt proto command
// Use this file to work on individual modules
// or build something new

// Root Element of the index.js file loaded by the prototype.
const rootEl = document.getElementById('root');

rootEl.innerHTML = ('Hello World');
